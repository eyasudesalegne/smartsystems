## Command reference

Deterministic: `/status`, `/health`, `/jobs`, `/logs`, `/note`, `/remind`, `/reminders`, `/today`, `/digest`, `/queue`, `/dequeue`, `/rbac`, `/approvals`, `/socialapprovals`, `/lead`, `/account`, `/deliverables`, `/idea`, `/collectassets`, `/publish`, `/socialstats`.

AI-assisted with grounded retrieval: `/research`, `/methods`, `/paper`, `/manuscript`, `/triage`, `/meetingprep`, `/followup`, `/draftpost`, `/reliability`, `/compare`, `/datasetqa`, `/run`, `/publishbundle`, and plain non-command chat.
