## Observability

Implemented observability in this package includes: health and readiness endpoints, queue depth metrics, aggregate `/metrics`, persisted `analytics_snapshots`, persisted `reliability_snapshots`, AI latency capture in `ai_output_artifacts`, and run/error histories through `job_runs` and `queue_attempts`.
