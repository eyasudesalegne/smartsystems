## Approval governance

Social approvals and publication approvals are distinct. Draft posts and manuscripts create pending approval rows. `POST /approvals/transition` enforces domain scopes and optionally reviewer separation when encoded in approval metadata or policy. Publication bundles can require approval before becoming ready.
